import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Keep the default Vercel Next.js build. `output: "export"` is not needed
  // and can drop App Router metadata so `/` 404s on a static host.
};

export default nextConfig;
